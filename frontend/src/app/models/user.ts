export interface User {
  firstName: string;
  lastName: string;
  username: string;
  password: string;
  role: String;
  email: String;
  phoneNum: String;
  profilePic?: string; // optional profile picture
  // add more properties as needed
}
